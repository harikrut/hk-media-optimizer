<?xml version="1.0"?>
<ruleset name="HK Media Optimizer">
	<description>Coding standards for HK Media Optimizer.</description>

	<!-- What to scan. -->
	<file>.</file>
	<exclude-pattern>/vendor/</exclude-pattern>
	<exclude-pattern>/node_modules/</exclude-pattern>
	<exclude-pattern>/dist/</exclude-pattern>

	<!-- Only scan PHP files. -->
	<arg name="extensions" value="php"/>

	<!-- Show sniff codes and progress. -->
	<arg value="ps"/>
	<arg name="colors"/>

	<!-- WordPress coding standards. -->
	<rule ref="WordPress">
		<exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
		<exclude name="WordPress.Files.FileName.InvalidClassFileName"/>
		<exclude name="Generic.Arrays.DisallowShortArraySyntax.Found"/>
	</rule>

	<!-- Verify text domain. -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="hk-media-optimizer"/>
			</property>
		</properties>
	</rule>

	<!-- Verify a consistent prefix on globals. -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="hkmo"/>
				<element value="HKMO"/>
			</property>
		</properties>
	</rule>

	<!-- Minimum supported WordPress version for deprecation checks. -->
	<config name="minimum_wp_version" value="5.8"/>

	<!-- PHP cross-version compatibility (7.4 and up). -->
	<rule ref="PHPCompatibilityWP"/>
	<config name="testVersion" value="7.4-"/>
</ruleset>
